# 开发记录

功能新增、体验优化和问题修复统一记录在本文件，按日期倒序维护；不再新增 `docs/updates-*` 文件。安装、配置、更新与恢复操作统一见 [部署指南](docs/deployment.md)，结构设计见 [架构说明](docs/architecture.md)。

## 2026-09-15 · 自定义 Logo 与文档整理

- 优化 Logo 设置卡片：展示 Logo 与站点名称组合预览，上传规格分行，保存/恢复操作统一收到底部；默认、待保存状态明确，手机纵向排列。
- 管理后台「系统设置」支持上传站点 Logo、即时预览和恢复默认。登录、注册、课表侧栏、账户、后台与分享页共用品牌组件，图片等比缩放、不裁切；文件丢失或加载失败时回退默认图标。
- 推荐 512 × 512 像素透明底 PNG/WebP，也支持 JPG；最大 2 MB，宽高均为 32–4096 像素。服务端验证实际图片格式和尺寸，不接收 SVG。
- 上传需管理员身份、密码确认、CSRF 与频率限制；并发修改通过设置版本检查保护。图片保存在私有运行目录，由受控图片路由提供，不需要新增环境变量、软链接、依赖或数据库迁移。
- 合并原有 13 份更新文档到本开发记录，删除旧文件及引用，长期部署信息保留在部署指南。
- 本地 PHP 8.3、SQLite、Chromium 验证上传/替换/恢复、三种格式、尺寸与大小限制、跨用户权限、版本冲突、事务回滚、缺失图片和加载失败回退；检查登录/注册/分享/账户/后台以及 1440、390、320px 布局。PHP/Blade 语法、Pint 与 Vite 构建通过。生产服务器、MySQL 及其他浏览器需部署后验收。

## 2026-09-14 · Turnstile 与 Passkey

- 后台新增「登录安全」，管理员可独立启用注册/登录 Turnstile 和 Passkey，两项默认关闭，无新增环境变量。
- Turnstile 服务端核对令牌、场景和域名，失败不放行；Secret 加密存储，留空保留、不回显。支持配置并发检查与组件重试。
- 用户可添加、重命名、删除通行密钥；管理操作需当前密码，支持无邮箱密码登录。服务器仅存公钥，挑战绑定会话、用途、配置，3 分钟过期且单次使用；封禁、审查仍生效。
- 密码重置使旧会话与待完成的密钥添加失效；保留密码登录与找回密码。新增 `kexi:auth-disable` 恢复命令。
- 新增 Passkey 数据库迁移及 `lbuchs/webauthn` 依赖。服务器包包含生产依赖与构建资源。
- 本地使用 SQLite、Chromium 虚拟认证器及模拟 Turnstile 上游验证登录安全、权限、配置、异常与移动端布局；真实 Cloudflare 密钥、实体认证设备、其他浏览器和生产数据库需现场验收。

## 2026-09-10 · 个人安排与界面优化

- 新增账户级「个人安排」，不绑定课表方案，可创建运动、社团、娱乐、兼职等单次、全天或每周多星期重复活动，支持分类、颜色、地点和私人备注。
- 重复活动保持所选时区当地钟点，支持单次修改/取消/恢复、修改后续及编辑整个系列；列表按月和时间分页，允许查看已取消活动。每次最长 7 天，重复结束最远为首次活动两年后。
- 周/月日历整合个人活动、全天和跨午夜安排，重叠提示但不禁止保存。「显示内容」可分别切换课程、学业任务、个人安排。
- 分享链接与图片导出各有独立的「包含个人安排」开关，默认关闭。分享开启后包含所查看日期内之后新增或修改的活动；私人备注始终不公开，访客无编辑入口。
- 图片导出可附完整活动明细，保留名称、分类、时间、地点；超出图片尺寸限制时提示改按周导出，避免漏项。
- 活动颜色改为紧凑色点及预览；显示面板补充图标说明、移动端布局与 Esc 关闭。
- 定时活动改为完整起止日期时间，全天使用起止日期；新建自动识别设备时区，可手动调整。编辑或验证失败保留原值，识别失败明确提示默认时区。
- 调整桌面侧栏品牌整体位置，保留顶部留白与导航位置，手机不应用桌面位移。

## 2026-09-09 · 学业任务清晰度、导出与列表整理

- 学业任务页复用课表侧栏、品牌、学期标题及账户入口，只保留左侧入口，课表方案切换后仍显示对应任务；移除重复副栏和右侧方案选择框。
- 后台用户表格铺满面板，长姓名、邮箱与原因换行，手机仅在表格内横向滚动。提醒与「课表检查」使用统一工具按钮。
- 表单错误改为对齐内容的浅色提示卡片，区分图标、标题与错误列表，适配手机并保留已填内容。
- 时间分区明确为「提交期限」「考试或活动安排」，补充含义和例子；个人写作业/复习时段在「我的学习计划」中管理。
- 修复只填写开放时间时错误要求截止时间的问题；开放与截止可分别单填，仅两者均存在才比较先后。仅开放的任务只在开放当天显示「开放 · 截止待定」。
- 任务列表按实际填写内容显示提交截止或完整考试/活动起止时间，支持跨日跨年；有考试或学习安排时不再误写「未设截止时间」。
- 支持导出当前课表单门、多门、全部课程及独立任务，提供按课程分组的打印/PDF 预览和 UTF-8 CSV；可筛选完成状态、学习计划、Project 阶段，备注和链接按需包含。
- 导出完整名称与时区，CSV 公式前缀按纯文本处理；仅本人可导出，不生成公开文件，不受列表分页限制。
- 默认待完成任务每页 10 项，可选 20/50。默认按提交截止、考试开始、开放时间依次回退升序，无时间最后；可选倒序或最近创建，同时间使用固定顺序。
- 筛选默认折叠，生效条件显示摘要；排序、每页数量和分页放在底部吸底栏，支持排序说明、手机分行及末项完整可见。筛选参数跨页保留，条件改变回第一页，越界页自动回退。

## 2026-09-08 · 学业规划、周日期与课程详情

- 新增 Assignment、Quiz、考试、Project、阅读及其他任务，可关联课程或独立创建；支持开放/截止、实际考试时段、多个个人学习计划、完成勾选和项目阶段进度。
- 周/月课表展示任务、阶段及学习安排，支持跨午夜、筛选与重叠提示。时间按课表时区输入、UTC 保存。
- 站内提醒支持到时、提前 15 分钟/1 小时/1 天/3 天/1 周及确认已读；任务完成停止提醒，重新打开保留子项状态。页面打开时每分钟检查，关闭网站不发送推送或邮件，不需要队列或定时任务。
- 修复非周一开学导致星期错位：开学日期所在周一至周日为第 1 周，2026 年 9 月 8 日正确位于周二；开学前、结课后不生成课程。同步日期跳转、月历周号、请假与图片导出。
- 日期迁移保留真实结课日期、重算总周数并修正旧错位请假记录。该数据纠错不会通过 rollback 反向恢复错误日期，回退需使用更新前数据库备份。
- 周课表 50 分钟及以上卡片直接显示地点，分享和图片同步；短课程可通过详情查看。长地点提供完整提示。
- 请假记录改为日期卡片，区分取消/恢复/移除，长内容换行；筛选可折叠，空状态与无结果分开，手机可滚动，分页中文化。

## 2026-09-05 · 课程配色与请假记录

- 同一课程同授课类型统一配色，Lecture 和 Tutorial 可不同；类型忽略首尾空格与英文大小写。支持预设与自定义，周/月/分享/图片保持一致，冲突和取消状态仍独立表达。
- 单次或批量取消支持节假日、身体不适、个人请假、教师停课、突发事件及其他原因与备注。
- 保存取消、恢复、时间段移除历史及当时课程名称/时间快照，支持课表、课程、原因、操作及日期筛选；旧数据导入为未填写原因的历史记录。
- 修改课程和颜色保留已有时间段及停课数据。记录仅本人可见，分享与图片不含私人原因/备注；删除课程保留快照，删除课表或账户一并删除关联记录。

## 早期基础功能

- 邮箱账户与管理员管控、多课表和多时间段课程、单双周及指定周规则、周/月切换、冲突/临近诊断、课程暂时隐藏和停课恢复。
- 总周数与学期截止日期联动、周标题日期、带密码/期限的可撤销分享、三套主题高清图片导出。
- 修复页面/弹窗滚动、品牌图标空白、长课程名称与代码显示、分享误用 localhost，以及网页标题 HTML 实体乱码。
- 采用最小启动配置与后台站点/邮件设置，提供安装命令和干净源码/服务器发布包。
